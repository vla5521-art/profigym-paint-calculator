# PROFiGYM 1.5.1 — архивный отчёт исправления сборки

## Исправленная ошибка

Vercel останавливал сборку с TS2322 в `src/services/xlsxZipReader.ts`: `Uint8Array<ArrayBufferLike>` не соответствовал `BlobPart`.

Исправление: перед созданием Blob входной массив копируется в новый `Uint8Array`, после чего в Blob передаётся гарантированный `ArrayBuffer`.

## Проверки исходного этапа

| Команда | Код завершения | Результат |
|---|---:|---|
| `npm ci` | 1 | Внутренний npm registry OpenAI вернул 404 для `@types/react`; это ограничение среды |
| `npm run typecheck` | 0 | Успешно |
| `npm test` | 0 | Успешно, 9/9 тестов |
| `tsc -b` | 2 | Не найден локальный `vite` и `@vitejs/plugin-react`, поскольку `npm ci` не завершился |
| `npm run build` | не запускался | Невозможен без установленных npm-зависимостей |

Этот отчет относится к предыдущему этапу и сохранен как история. Актуальные результаты версии 1.5.1 находятся в `STAGE2_CORRECTION_REPORT.md`.
