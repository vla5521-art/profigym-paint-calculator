import fs from 'node:fs/promises'; import path from 'node:path'; import { cadConfig } from '../server/config.js'; import { migrateDatabase } from '../server/cad/calculations/migrations.js'; import { root, sha256File, writeJson } from './quality-utils.mjs';
const database=migrateDatabase(cadConfig.databasePath);database.exec('PRAGMA wal_checkpoint(TRUNCATE)');database.close();const backup=path.join(root,'.tmp/backups/calculations.sqlite');await fs.mkdir(path.dirname(backup),{recursive:true});await fs.copyFile(cadConfig.databasePath,backup);const report={ok:true,source:path.relative(root,cadConfig.databasePath),backup:path.relative(root,backup),sha256:await sha256File(backup)};await writeJson(path.join(root,'diagnostic-reports/database-backup.json'),report);console.log(JSON.stringify(report,null,2));
